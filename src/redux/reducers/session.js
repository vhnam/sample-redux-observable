const initialState = {};

const sessionReducer = (state = initialState, action) => {
  const {type} = action;
  switch (type) {
    default:
      return state;
  }
};

export default sessionReducer;
